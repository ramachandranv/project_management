= form_for @project do |f|
  =f.label :name
  =f.text_field :name
  %br
  =f.label :description
  =f.text_area :description
  %br
  - if type == 'new'
    =f.submit "Create Project"
  - else
    =f.submit "Update"
